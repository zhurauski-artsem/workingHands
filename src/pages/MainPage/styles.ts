import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  centeredText: {
    textAlign: 'center',
  },
  title: {
    fontSize: 22,
    fontWeight: 600,
    textAlign: 'center',
  },
});
